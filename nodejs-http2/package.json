{
  "name": "nodejs-http2",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js",
    "self-signed-cert": "openssl req -x509 -newkey rsa:4096 -nodes -sha256 -subj '/CN=localhost' -keyout localhost-private.pem -out localhost-cert.pem",
    "cert-bot": "sudo certbot certonly --standalone"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
